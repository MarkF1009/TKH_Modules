/* Setting up array for names */

const names = ["Westly Snipes", "Nicholas Cage", "Nasir Jones", "Sean Carter", "Sean Combs",
"Michael Jordan", "Patrick Ewing"]

/* We want two empty array that we are going to be filling up */

first_name = []
last_name = []

/* Now we have to loop thru all the names */

for (let i = 0; i < arr1.first_name; i++) {
  if (arr1[i] % 2 == = 0){
    first_name.push(arr1[1])
  }
  else {
        last_name.push(arr1[1])
  }
}
